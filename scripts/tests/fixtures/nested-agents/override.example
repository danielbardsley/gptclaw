# UI temporary override example

Source-Template-Version: 1.0.0
Template-Source: GptClaw templates/agents/nested/AGENTS.md.template

## Scope and ownership

Area-Scope: ui
Parent-Guidance: ../AGENTS.md
Owner: Synthetic fixture maintainer

This illustrative ui subtree contains documentation only. Follow the
[parent contract](../AGENTS.md); this example is inert until manually adapted.

## Local conventions

Use title case for UI labels during this synthetic experiment. Document keyboard behavior, focus, empty states, and error states alongside proposed interactions.
Apply these conventions only here; inspect deeper guidance before working there.

## Commands and verification

Review changed Markdown and its relative links using the [fixture guide](../README.md).
No setup, startup, build, or stack test applies: this fixture has no application.
Do not invent a command; an actual adoption must cite its own command sources,
working directories, prerequisites, side effects, and check selection.

## Constraints

Use synthetic content and existing assets with approved provenance; this example installs no UI framework or browser tooling.
Respect higher-priority instructions, existing authorization, and permissions.
This guidance grants no authority and is not a security boundary. Report
unresolved conflicts and continue unaffected work without repeated approvals.

## Maintenance

Temporary exception owner: Synthetic fixture maintainer. Scope: UI label case
in this fixture only. Reason: demonstrate override shadowing. Expiry: end of
the acceptance run. Removal: operator removes only the task-owned UI override,
then starts a fresh task to verify restoration. No production permission changes.

The synthetic fixture maintainer owns this example. Follow the
[fixture update and rollback procedure](../README.md). Keep within 4 KiB UTF-8;
version 1.0.0 records ancestry. Adapt through review, never auto-synchronize.
Verify changes or restoration in a fresh task and preserve unrelated work.
